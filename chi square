observed <- matrix(c(10, 20, 15, 25), nrow = 2, byrow = TRUE)
rownames(observed) <- c("Group A", "Group B")
colnames(observed) <- c("Category X", "Category Y")

chi_square_result <- chisq.test(observed)

print(chi_square_result)
