library(caret)
actual_values <- c("Yes", "No", "No", "Yes", "No")
predicted_values <- c("Yes", "Yes", "No", "Yes", "No")

conf_matrix <- confusionMatrix(predicted_values, actual_values)

print(conf_matrix)

confusionMatrixPlot(conf_matrix)
